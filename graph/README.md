graph generator

activate the virtual env: source bin/activate

Edit main.py and make sure that the month (on line 13) is set to current month.

run mkdir ../content/data/<Month> if the folder doesn't already exist

Run the script only after data exists for the whole month
